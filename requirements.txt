# Basic web framework
flask

# For AI model handling
torch
transformers
accelerate
# Add other specific versions if necessary, e.g., torch==1.9.0+cpu
# For Qwen2 model, specific dependencies might be needed depending on the exact model variant and quantization
# sentencepiece # Often required for tokenizers
